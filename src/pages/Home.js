import Section from '../Components/Section';
import Header from '../Components/Header';
const Home = () => {
    return (
        <div>
            <Header />
            <Section />
        </div>
    );
};

export default Home;
